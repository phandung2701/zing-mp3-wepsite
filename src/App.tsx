import React from 'react';
import Layout from './components/Layout';

interface AppProps {}

const App: React.FC<AppProps> = () => {
  return (
    <div>
      <Layout />
    </div>
  );
};

export default App;
